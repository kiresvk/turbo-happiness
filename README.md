# turbo-happiness
learning git and features :)
```javascript
function fancyAlert(arg) {
  if(arg) {
    $.facebox({div:'#foo'})
  }
}
```
```php
$inst = new ClassName();
$inst->Debug(TRUE);
$inst->Go();
```
```jquery
$('#element').hide();
```